__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
dist/
build/
.venv/
venv/

.env
.env.*
!.env.example

.idea/
.vscode/
*.swp
.DS_Store

# Terraform
infra/**/.terraform/
infra/**/*.tfstate
infra/**/*.tfstate.*
infra/**/.terraform.lock.hcl
infra/**/crash.log
# envs/*.tfvars are committed: they hold non-secret config (instance counts,
# allow_unauthenticated, secret *names*). Plaintext secret VALUES never go in
# Terraform — bootstrap.sh writes them to Secret Manager via gcloud.
infra/**/secrets.auto.tfvars
infra/**/*.tfplan
infra/**/*.tfplan.binary
infra/**/.terraform.tfstate.lock.info

# Secrets / credentials (catch-all for files that should never be committed)
*.pem
*.key
*-key.json
*credentials*.json
*service-account*.json
gha-creds-*.json
secrets.auto.tfvars
infra/**/secrets.auto.tfvars

# Notebook checkpoint dirs
.ipynb_checkpoints/

# IDE / OS extras
.cspell/
*.iml
